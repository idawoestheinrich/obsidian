## 🧠 What “multithreading” means — in plain English

**Multithreading** means running _multiple parts of a program at the same time_ — in parallel — on different CPU cores.

Each “thread” is like a **mini-program** inside your main program that can execute independently.

